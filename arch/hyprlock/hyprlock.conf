background {
    monitor =
    color = rgba(25, 20, 20, 1.0)
}

input-field {
    monitor =
    size = 250, 50
    position = 0, 0
    halign = center
    valign = center
    placeholder_text = Password...
    fade_on_empty = true
    rounding = 10
}

label {
    monitor =
    text = $TIME
    font_size = 50
    position = 0, 100
    halign = center
    valign = center
}
